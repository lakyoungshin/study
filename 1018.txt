크롬 뷰 설치 방법
chrome vue devtools 구긍 검색

펑션으로 시작하는 함수들은 맨위로 올라가는 
포이스티 형식이 발생한다.

html = background-color
js = backgroundColor

-가 대문자로 바뀐다 생각하면됨

splice 삭제할때 사용하는 것

이름과 값의 쌍 이 객체다
값에 
변수가 오면 프로퍼티
함수가 오면 메서드 

바벨은 컴파일러
웹팩은 합치는거~

함수가 들어있는 파일은 모듈
모듈이 있는건 패키지 (디렉토리)
이런 디렉토리가 많은건 라이브러리
라이브 러리가 많고 기능이 있는것은 프레임워크

라우팅은 앵커기능
리액트나 뷰는 html이 하나인데 
긴 html을 앵커기능 (한곳만 보여주는거)을 활용하여
부분부분을 보여준다고 이해하면됨

뷰는 리액트와 앵귤러의 좋은점을 가져온것
리엑트 > 컴포넌트
앵귤러 > 지시자 를 끌어옴

뷰 제일 중요한거
      el: "#app",
        data: {

        },
        methods: {
         이벤트 핸들러를 작성하는 부분   
	태그 속성을 작성  
       }
e,dt,mc2
뷰는 인라인 스타일로 먹음

제이쿼리랑 뷰랑 같이 써도 된다.
하지만 영역을 다르게 써야함.

vue의 양방향 바인딩은 v-model만

컴포넌트는 ?
쪼갠다!
블럭 형식으로 레고맞추듯 쪼개서 끼워맞춘다 라고생각하면됨

바벨은? 컴파일러
웹팩은? 파일을 합치는것
인스턴스는 (new Vue) ?
e/dt/mc*2w
땅을 만든다 라고 생각하면됨

그 안에 
변수부분
el
data
 
함수부분
methods : 이벤트 처리  
computed : 동기식 처리 - 그 이외 나머지 다 
watch : 비동기식 처리 -결과를 받기 전까지 기다린다 -아작스 통신

등등이 생기는것

뷰의 인스턴스 끼리 같은값을 공유할 수 없다,
같은 레퍼런스일때만 가능하다.

프로퍼티의 이름충돌을 피하기위해 $ 붙임

뷰셋은 프로토 타입
디스셋은 



 










