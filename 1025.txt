//화면은 컴포난트가 받고 컴포넌트를 조합하는건 인스턴스
//template: ``, *실제로 인스턴스에서 템플릿을 쓰지는 않음
컴포넌트
/* edtmc^2w */
/* pdtmc^2w */
스프레드 연산자가 인기가많다
v-on 이벤트를 실행시키는 것
keypress : 키보드를 눌렀다 떼는거
모든 인풋의 정보는 이벤트 타겟에 들어가 있다.



메소드는 동기식
와치는 비동기식
책 -106p-109p
대괄호 사이에 있는것은 데이터에 등록해야한다

v-on만 메소드에 등록하고
대쉬랑 나머지는모두 데이터에 등록해야함

computed는 한번만 쓸 수 있다 
동시 사용 불가능 
computed는 반드시 값을 반환해야 한다 (return함수)
컴퓨티트에 현재시간 추가하려면?
const dt = new Date().toLocaleString();
return this.$data.message=""~~+dt 
이런식으로
동기식의 값이 바뀌면 자동으로 입력된다


watch함수 (비동기식함수)
모니터링 하고싶은 함수를 꼭 앞에 쓰도 실행해야한다
this.$data.watchcopy = val; == Vue.set (this.$data, "watchcopy" ,val) 
*회원가입시 아이디 중복 같은거 와치함수쓰면 빨리해결됨*

지시자
v-on : 한방향 바인딩 ("모든함수")
v-bind :value빼고 나머지 다됨 (벨루도 되긴하나,, 거의 v-model로 씀)
v-model  :양방향 바인딩 (인풋, 셀렉트, 텍스트에리어,벨류)//화면에서 입력받아야하는것만 v-model
v-if
v-else
v-for

라이프사이클 속성
update : 업데이트할때마다 수시로
mounted: 페이지 로딩시 한번만
//마운티드에서 값을바꿔도 업데이트가 호출된다

태그 사이에 템플릿을 넣는것이 인라인이다.

scoped >> 이 한곳에서만 이것을 먹게 하겠다.

v-if / v-else 
디스플레이 넌 블락과 같은 맥락
하지만 이건 태그를 아예 지우는것
v-for 반복하여 사용하는 태그

 v-model 속성은 v-bind와 v-on의 조합으로 동작합니다.
 ex) <input v-bind:value="inputText" v-on:input="updateInput">

104p 표 참고
v는 무조건 데이타에
v-for 는 in 다음에 오는 변수만 데이터로 등록한다 
이 다음에 오는 v- bind의 변수는 데이터로 등록되지 않고 인 앞에있는게 무엇인지를 의미함

102p 데이터 바인딩
콧수염 표현식 자바스크립트 쓸수 있는 방법
이벤트 취소 방법은 리턴 펄스는 쓸수 없고
프리벤트 디폴트나, 스탑프로퍼게이션 쓸수 있다.

v-bind 다중클라스는 트루 펄스로 처리한다

커밋하는 방법
공유버튼 점세개 커밋 다음에
푸시 > 취소 > 아이디 > 패스워드

